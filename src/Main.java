import java.util.ArrayList;

public class Main {
    public static void main(String[] args) throws Exception {
        EDA baca = new EDA();
        Fuzzy f = new Fuzzy();

        // ArrayList<Double[]> simpan = new ArrayList<>();
        // baca.bacaData("data", simpan);
        // baca.printData(simpan);

        System.out.println(f.hujanRendah(1.5));

    }
}
